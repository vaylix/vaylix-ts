{
  "name": "@vaylix/client",
  "version": "0.4.0",
  "description": "Official TypeScript client for the Vaylix database.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vaylix/vaylix-ts.git"
  },
  "keywords": [
    "vaylix",
    "database",
    "db",
    "client",
    "sdk",
    "typescript",
    "nodejs",
    "tcp",
    "kv",
    "key-value"
  ],
  "homepage": "https://vaylix.github.io",
  "bugs": {
    "url": "https://github.com/vaylix/vaylix-ts/issues"
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "provenance": true,
    "access": "public"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "!dist/*.tsbuildinfo",
    "README.md"
  ],
  "scripts": {
    "dev": "pnpm exec concurrently -k -n bundle,types -c cyan,magenta \"pnpm exec tsup --config tsup.config.ts --watch\" \"pnpm exec tsc -p tsconfig.types.json --watch --preserveWatchOutput\"",
    "build": "tsup --config tsup.config.ts && tsc -p tsconfig.types.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts",
    "test:live": "node --import tsx test/integration.live.ts",
    "prepack": "pnpm --dir ../.. --filter @vaylix/scripts sync-package-readme"
  },
  "dependencies": {
    "@mongodb-js/zstd": "^7.0.0",
    "crc-32": "^1.2.2",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
